"""Legacy foundation schemas (placeholder). Add small, shared models here; keep isolated."""
# from pydantic import BaseModel  # enable when needed
# class LegacyExample(BaseModel):
#     name: str
"""Legacy foundation schemas (placeholder). Add small, shared models here; keep isolated."""
# from pydantic import BaseModel  # enable when needed
# class LegacyExample(BaseModel):
#     name: str
