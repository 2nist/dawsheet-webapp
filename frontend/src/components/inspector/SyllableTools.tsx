export function SyllableTools() {
  return (
    <div style={{ display: 'flex', gap: 6, marginBottom: 8 }}>
      <button
        onClick={() => {
          /* split stub */
        }}
      >
        Split syllables
      </button>
      <button
        onClick={() => {
          /* merge stub */
        }}
      >
        Merge syllables
      </button>
    </div>
  )
}
